
import { Item, Review, User } from './types';

// Banco de dados inicial vazio para garantir segurança total
export const MOCK_USERS: User[] = [];
export const MOCK_ITEMS: Item[] = [];
export const MOCK_REVIEWS: Review[] = [];
